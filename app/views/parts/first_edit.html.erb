<%= form_with url: part_first_path(@part), method: :put, local: true do |form| %>
  <%= form.label :first_assessment %>
  <%= form.text_area :first_assessment, size: "100x10", value: @part.first_assessment %><br>
  <%= form.submit "Save assessment" %>
<% end %>
