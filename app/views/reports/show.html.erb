<div class="report_sections">
  <% @report.sections.each do |section| %>
    <%= link_to "#{section.name}", section_path(section) %>
  <% end %>
</div>
<div class="additional_info">
  <%= link_to "Additional Documents", report_additional_documents_path(@report) %>
</div>
